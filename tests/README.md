# Tests

From the repository root:

```bash
bash tests/setup/run.sh
bash tests/lint/run.sh
```

Setup tests cover binding setup, runtime state, registry output, and native starter creation.

Lint tests cover protocol lint and native lint.
