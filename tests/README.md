# Tests

Regression checks live here.

Run lint fixtures with:

```bash
bash tests/lint/run.sh
```

The lint fixture runner creates temporary wiki repos at runtime so the checked-in template remains the single source of truth.
