# Schema

## Domain
Separate domain used to prove the fixture can contain more than one domain.

## Tag Taxonomy
- other

Rule: tags are coarse domain classification labels; keep them to 1-3 per page instead of keyword stacks.
