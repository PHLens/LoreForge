interface:
  display_name: "LoreForge Router"
  short_description: "跨 domain 选择、查询与 ingest 调度入口"
  default_prompt: "Use $loreforge-router to route this request to the right LoreForge domain expert(s), then delegate domain work through $loreforge-wiki."
