[project]
name = "convert-to-markdown"
version = "0.1.0"
description = "Universal document to Markdown converter"
requires-python = ">=3.8"
dependencies = [
    "mammoth>=1.8.0",
]

[project.scripts]
convert-to-markdown = "convert_to_markdown:main"