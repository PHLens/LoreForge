[project]
name = "topic-research"
version = "0.1.0"
description = "Generic web anti-bot, risk-control, and captcha investigation tooling"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.58.0",
    "playwright-stealth>=2.0.2",
    "requests>=2.32.0",
]

[project.scripts]
topic-research = "main:main"
