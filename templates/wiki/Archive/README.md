# Archive

Closed packages and exceptional retired material.

Promotion moves consumed packages to `promoted/`. Rejected packages move to `rejected/`. Stable note history should normally rely on Git history rather than copied archives.
