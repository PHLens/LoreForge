# Inbox

Temporary location for staged ingest and writeback packages.

Use this area when knowledge may be durable but still needs review before stable placement.

## Layout

```text
ingest/       Staged packages from external sources
writeback/    Staged packages from conversation/query synthesis
```

Packages should include `manifest.md` plus candidate notes or deltas.
