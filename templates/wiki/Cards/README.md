# Cards

Stable atomic knowledge notes.

Cards are flat. Do not create domain or migration batch subdirectories here.

Every stable card must appear in `00_System/+Wiki Index.md`. MOCs, `up`, `X::` links, and inbound links are semantic integration signals, not required initial placement.
