# Sources

Source-grounded notes organized by source type.

Use source type directories such as `Papers/`, `Articles/`, `Docs/`, and `Repos/`. Do not organize sources by domain or import system. Record import systems such as Cubox, Zotero, GitHub, or local files in source frontmatter.
